// Backward-compatible re-export from refactored engine modules
export {
  applyConflictResolution,
  pullOnly,
  pushOnly,
  syncCycle,
  type SyncCycleResult,
  type SyncEngineOptions,
} from './engine/index'
